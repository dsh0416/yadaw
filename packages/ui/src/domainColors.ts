/**
 * Stable serialized colors for DAW domain objects.
 *
 * These values are data defaults rather than component styling. They mirror the
 * CSS domain palette and remain theme-independent so saved projects are portable.
 */
export const UI_DOMAIN_COLORS = {
  audioChannel: "#4F8CFF",
  busChannel: "#E8B85F",
  masterChannel: "#8C83FF",
  outputChannel: "#EF7C95"
} as const
