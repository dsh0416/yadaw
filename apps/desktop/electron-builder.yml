appId: dev.yadaw.studio
productName: YADAW
# AppImage rejects `@` from the scoped package name `@yadaw/desktop`.
executableName: yadaw
asar: true
asarUnpack:
  - "**/*.node"
  - "**/@electric-sql/pglite/dist/*.wasm"
  - "**/@electric-sql/pglite/dist/*.data"
npmRebuild: false

directories:
  output: ../../release

artifactName: "${productName}-${version}-${os}-${arch}.${ext}"

files:
  - out/**/*
  - build/icon.png
  - package.json
  - node_modules/**/*
  - "!node_modules/@yadaw/dsp-node/src/**/*"
  - "!node_modules/@yadaw/dsp-node/Cargo.toml"
  - "!node_modules/@yadaw/dsp-node/build.rs"
  # Build-only native parsers pulled by Unhead/Unplugin must not enter universal packages.
  - "!node_modules/@oxc-parser/**/*"
  - "!node_modules/oxc-parser/**/*"
  - "!node_modules/@unhead/bundler/**/*"

win:
  icon: build/icon.svg
  extraResources:
    - from: ../../target/release/yadaw-audio-host.exe
      to: yadaw-audio-host.exe
    - from: ../../target/release/yadaw-vst3-probe.exe
      to: yadaw-vst3-probe.exe
    - from: ../../target/bundles
      to: plugins
      filter:
        - YADAW Gain.vst3/**/*
        - YADAW Metronome.vst3/**/*
        - YADAW Sine.vst3/**/*
  target:
    - nsis

mac:
  icon: build/icon.svg
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extraResources:
    - from: ../../target/release/yadaw-audio-host
      to: yadaw-audio-host
    - from: ../../target/release/yadaw-vst3-probe
      to: yadaw-vst3-probe
    - from: ../../target/bundles
      to: plugins
      filter:
        - YADAW Gain.vst3/**/*
        - YADAW Metronome.vst3/**/*
        - YADAW Sine.vst3/**/*
  target:
    - dmg

linux:
  icon: build/icon.svg
  extraResources:
    - from: ../../target/release/yadaw-audio-host
      to: yadaw-audio-host
    - from: ../../target/release/yadaw-vst3-probe
      to: yadaw-vst3-probe
    - from: ../../target/bundles
      to: plugins
      filter:
        - YADAW Gain.vst3/**/*
        - YADAW Metronome.vst3/**/*
        - YADAW Sine.vst3/**/*
  target:
    - AppImage
