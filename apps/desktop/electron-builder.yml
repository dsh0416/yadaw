appId: live.minori.heron
productName: Heron
# AppImage rejects `@` from the scoped package name `@heron/desktop`.
executableName: heron
asar: true
asarUnpack:
  - "**/*.node"
  - "**/@electric-sql/pglite/dist/*.wasm"
  - "**/@electric-sql/pglite/dist/*.data"
npmRebuild: false

directories:
  output: ../../release

artifactName: "${productName}-${version}-${os}-${arch}.${ext}"

files:
  - out/**/*
  - build/icon.png
  - package.json
  - node_modules/**/*
  - "!node_modules/@heron/dsp-node/src/**/*"
  - "!node_modules/@heron/dsp-node/Cargo.toml"
  - "!node_modules/@heron/dsp-node/build.rs"
  # Build-only native parsers pulled by Unhead/Unplugin must not enter universal packages.
  - "!node_modules/@oxc-parser/**/*"
  - "!node_modules/oxc-parser/**/*"
  - "!node_modules/@unhead/bundler/**/*"

win:
  icon: build/icon.svg
  extraResources:
    - from: ../../THIRD_PARTY_NOTICES.txt
      to: THIRD_PARTY_NOTICES.txt
    - from: ../../target/release/heron-audio-host.exe
      to: heron-audio-host.exe
    - from: ../../target/release/heron-vst3-probe.exe
      to: heron-vst3-probe.exe
    - from: ../../target/bundles
      to: plugins
      filter:
        - YADAW Gain.vst3/**/*
        - YADAW Metronome.vst3/**/*
        - YADAW Sine.vst3/**/*
  target:
    - nsis

mac:
  icon: build/icon.svg
  hardenedRuntime: true
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extraResources:
    - from: ../../THIRD_PARTY_NOTICES.txt
      to: THIRD_PARTY_NOTICES.txt
    - from: ../../target/release/heron-audio-host
      to: heron-audio-host
    - from: ../../target/release/heron-vst3-probe
      to: heron-vst3-probe
    - from: ../../target/bundles
      to: plugins
      filter:
        - YADAW Gain.vst3/**/*
        - YADAW Metronome.vst3/**/*
        - YADAW Sine.vst3/**/*
  target:
    - dmg

linux:
  icon: build/icon.svg
  extraResources:
    - from: ../../THIRD_PARTY_NOTICES.txt
      to: THIRD_PARTY_NOTICES.txt
    - from: ../../target/release/heron-audio-host
      to: heron-audio-host
    - from: ../../target/release/heron-vst3-probe
      to: heron-vst3-probe
    - from: ../../target/bundles
      to: plugins
      filter:
        - YADAW Gain.vst3/**/*
        - YADAW Metronome.vst3/**/*
        - YADAW Sine.vst3/**/*
  target:
    - AppImage
